# База сравнения для эксперимента с аллокатором (12.09.2026)

## Режим A — pymalloc (до эксперимента)
- env: `MALLOC_ARENA_MAX=1`, `MALLOC_TRIM_THRESHOLD_=131072`, без PYTHONMALLOC
- замер: RSS 68 МБ (старт) → **131 МБ через 120 мин** = **31.5 МБ/ч**
- регионы: heap 67 МБ, анонимные арены 38 МБ
- производительность: msg/s 29.7-44.3
- подтверждено ранее: 24 МБ/ч в тишине, до 99 МБ/ч под нагрузкой (всплесковый рост)

## Режим B — PYTHONMALLOC=malloc (эксперимент)
- добавить `export PYTHONMALLOC=malloc` в start.sh + рестарт
- что ожидаем: аллокации идут через glibc; с одной ареной и trim-порогом
  память должна возвращаться ОС → рост МБ/ч меньше, чем 31.5
- что проверяем кроме памяти: msg/s и p50-латентность НЕ должны деградировать
  (pymalloc быстрее glibc на мелких объектах — плата за эксперимент)
- откат: закомментировать строку в start.sh, `bash p2p_daily_restart.sh`

## Как меряем
`mem_watch.sh` (nohup + init.sh) пишет `mem_watch.jsonl` каждые 5 мин:
режим, uptime, RSS, heap, арены, msg/s. Сравнение — по наклону RSS(мин).
