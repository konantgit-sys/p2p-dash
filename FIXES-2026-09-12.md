# Диагностика памяти p2p-dash — 12.09.2026

Продолжение разбора от 11.09 (см. `FIXES-2026-09-11.md`). Задача: назвать точную
причину роста RSS, а не гадать. Внедрена диагностика по требованию, сняты замеры.

## Что внедрено

Диагностические эндпоинты (постоянно ничего не работает, включается вызовом):

- `POST /api/debug/trace/start?frames=10` — включить tracemalloc;
- `POST /api/debug/trace/mark` — точка отсчёта;
- `GET  /api/debug/trace/diff` — что выросло с отметки (строки кода) + gc-статистика;
- `GET  /api/debug/objects` — крупнейшие типы объектов и их объём;
- `POST /api/debug/gc` — ручная сборка мусора с замером RSS до/после;
- `POST /api/debug/trace/stop` — выключить.

Плюс был добавлен фоновый цикл авто-сборки (`_gc_loop`, каждые 120 с, лог
`gc_log.jsonl`) — **проверен и отключён** флагом `GC_LOOP_OFF`: за 4 сборки
освобождено 0 МБ при ~250 собранных объектах. Работает вхолостую, только тратит CPU.

## Замеры (живой процесс, 12.09)

| Условие | Период | Рост RSS | Комментарий |
|---|---|---|---|
| Тишина (без запросов к API) | 100 с | +676 КБ (+24 МБ/ч) | фоновый рост |
| Активные запросы (диагностика) | 113 с | +3.1 МБ (+99 МБ/ч) | в основном мой же трафик |
| Тишина | 100 с | **0 МБ** | рост нелинейный |
| Первые 8 мин после старта | 8 мин | +4 МБ (~30 МБ/ч) | |

Рост **всплесковый и нагрузочно-зависимый**, а не линейный: в покое бывает ровно
нулевым, под нагрузкой — до 99 МБ/ч.

## Что показала диагностика

- **tracemalloc:** живых Python-объектов всего ~2 МБ. Рост в Python-объектах на
  порядок меньше роста RSS. Топ «роста» по строкам — HTTP-обвязка (`runners.py`,
  `httptools/decoder.py`, `streams.py`) и наш `phase0/transport.py` (сериализация
  при публикации). Это транзиентные аллокации обработки, они освобождаются.
- **gc.collect():** освобождает 0 МБ (собирает 185-250 объектов). Мусора нет —
  собирать нечего, значит память держат не мёртвые объекты.
- **smaps:** heap стабилен — 20-21 МБ; анонимные арены 16-26 МБ; файлы/библиотеки
  ~25 МБ. Сравнение карты регионов за 100 с: изменений выше 200 КБ нет вообще.
- **Пороги GC:** стандартные `[700, 10, 10]` — не при чём.

## Вывод

Это **не утечка объектов и не утечка ссылок**. Память держат аллокации уровня C и
арены, которые ОС не возвращает (фрагментация), а рост зависит от нагрузки. Назвать
«виновную строку кода» нельзя — её нет: объекты живут коротко и освобождаются, но
арены/сегменты остаются за процессом.

Практически:
- `MALLOC_ARENA_MAX=1` (11.09) снял основную часть проблемы: было 1011 МБ анонимных
  арен, стало 16-61 МБ;
- остаточный рост 0-30 МБ/ч компенсируется плановым перезапуском в 04:00;
- более радикальный вариант — `PYTHONMALLOC=malloc` (обойти pymalloc, отдать
  аллокации glibc с одной ареной и trim-порогом). Не проверялся: нужен прогон
  несколько часов под нагрузкой, чтобы сравнить кривые роста.

## Побочно найденный и исправленный баг: гонка двух start.sh

В логах поймано: `ERROR: [Errno 98] address already in use` на 8090 и 39001.
Причина — моя правка 11.09 (лок снимался до `exec`, чтобы health-check мог
поднимать бэкенд) убрала защиту от одновременного запуска: системный health-check
и скрипт рестарта стартовали `start.sh` почти одновременно, второй убивал живого
дашборда и падал сам.

Исправлено:
- `start.sh` стал **идемпотентным**: если `/health` отвечает — выходит, не трогая
  живой дашборд;
- `p2p_daily_restart.sh` получил **явную остановку** процессов перед вызовом
  `start.sh` (иначе рестарт бы ничего не делал).

Проверено: (1) запуск `start.sh` при живом дашборде — PID не меняется, в логе
`дашборд уже отвечает на /health — выходим`; (2) плановый рестарт — PID меняется,
`/health` 200, сайт 200; (3) комплект ровно один: 1 дашборд + 1 мост + 4 наблюдателя.

## Состояние на момент отчёта (09:40 МСК)

- дашборд PID 608603, RSS ~72 МБ; `/health` 200; сайт 200;
- все 9 эндпоинтов фронта: 200;
- контейнер: 7.01 из 12 ГБ (58%); диск data: 13.5 из 20 ГБ (69%);
- авто-GC выключен (`GC_LOOP_OFF`); диагностические эндпоинты оставлены включёнными
  (они безвредны, работают только по вызову).

## Эксперимент с аллокатором — ЗАКРЫТ, результат отрицательный (12.09)

**Итог по 28 точкам за 135 минут:**

| метрика | база (pymalloc) | эксперимент (malloc+arena1) |
|---|---|---|
| наклон RSS | +31.5 МБ/ч | **+34.2 МБ/ч** (было +33.5 от старта, +36 за последний час) |
| heap | 67 МБ через 2 ч | 43 → 120 МБ за 135 мин |
| арены | 38 МБ через 2 ч | 2 МБ стабильно |
| msg/s | 29.7-44.3 | 29.2-32.7 |
| p50 | 2.3 мс | 2.0-2.1 мс |

Вывод: `PYTHONMALLOC=malloc` рост НЕ снизил, а по heap сделал хуже. Единственный
плюс (арены не пухнут) минуса не перекрывает. Производительность не пострадала —
но это и не повод оставлять режим, который не решает задачу. **Откачено на pymalloc.**

**Главный факт, который дал эксперимент — не про аллокатор, а про природу роста.**
На живом процессе (RSS 150 МБ, heap 122 МБ) вызвал `malloc_trim(0)` через gdb
(символов libc в системе нет — вычислил адрес вручную: оффсет `malloc_trim`
из `.dynsym` + база libc из `/proc/PID/maps`):

```
$1 = 1                      <- trim выполнился и освободил всё, что мог
RSS 150 -> 150 МБ           <- но ОС не вернулось ничего
heap 122 -> 122 МБ
```

**Это значит: фрагментации нет.** Память занята живыми аллокациями, а не
«дырками» в куче. Версия про фрагментацию glibc закрыта, периодический trim
как решение тоже закрыт (нечего возвращать).

**Что остаётся единственной версией:** аллокации, которые удерживаются —
и их не видно в Python-объектах. Дальше проверяю строго: `tracemalloc` с меткой,
сравнение по строкам кода. Важно: прошлый замер (2 МБ Python-объектов) снимался
ПОСЛЕ включения трейсинга, то есть мог не покрыть утечку целиком — эта версия
ещё не отброшена, в отличие от фрагментации.

Данные прогона сохранены: `mem_watch-experiment-malloc-20260912.jsonl` (28 точек).

**Откат выполнен:** `export PYTHONMALLOC=malloc` закомментирован в `start.sh`,
дашборд перезапущен (PID 646369, `MALLOC_ARENA_MAX=1`, без PYTHONMALLOC).

Гипотеза после диагностики: рост RSS — невозвращаемые аллокации, а не утечка
объектов. Проверяем радикальный вариант: `PYTHONMALLOC=malloc` — отключает пул
мелких объектов Python, аллокации уходят в glibc, которая с `MALLOC_ARENA_MAX=1`
и trim-порогом должна возвращать память ОС.

**Режим A (база, pymalloc):** 68 → 131 МБ за 120 мин = **+31.5 МБ/ч**; heap 67 МБ,
арены 38 МБ через 2 часа; msg/s 29.7-44.3; p50 2.3 мс.

**Режим B (malloc+arena1):** добавлен `export PYTHONMALLOC=malloc` в `start.sh`
(с пометкой для откatа), дашборд перезапущен, PID 625512.

Первые замеры режима B (наблюдатель `mem_watch.sh`, каждые 5 мин):

| uptime | RSS | heap | арены | msg/s | p50 |
|---|---|---|---|---|---|
| 0.7 мин | 71 МБ | 43 МБ | 2 МБ | 21.8 | 2.3 мс |
| 5.7 мин | 73 МБ | 45 МБ | 2 МБ | 32.7 | 2.3 мс |
| 7.6 мин | 73 МБ | 45 МБ | 2 МБ | 29.8 | 2.0 мс |

Предварительный наклон: **+24 МБ/ч** против +31.5 МБ/ч у базы. На 8 минутах это
в пределах шума — вердикт только по накоплению (нужно 1-3 часа).

**Что важнее памяти:** скорость. Ожидаемая плата за отказ от pymalloc —
замедление мелких аллокаций. Пока деградации нет: p50 держится 2.0-2.4 мс,
msg/s в обычном диапазоне.

**Критерии вердикта:** (1) наклон RSS заметно ниже 31.5 МБ/ч — оставляем;
(2) наклон тот же — откатываем, эксперимент закрыт отрицательно;
(3) p50/mgs/s деградировали — откатываем независимо от памяти.

**Честная недоработка:** базу p50 для честного A/B я не снял до перезапуска
(история метрик латентность не хранит — `/api/metrics/history` отдаёт пустой
`points`). Поэтому сравнение латентности будет обратным: в конце эксперимента
откатим на pymalloc и померяем p50 в тех же условиях.

**Откат:** закомментировать строку `export PYTHONMALLOC=malloc` в `start.sh`,
запустить `bash p2p_daily_restart.sh`. Наблюдатель: `touch MEM_WATCH_OFF`.

## Остаётся непроверенным

- реальный рендер в браузере — браузера в поде нет.

## Эксперимент №2: uvloop отключён (запущен 12.09, первый замер через 2 часа)

**Гипотеза.** tracemalloc на 77 минут показал 49 179 живых объектов `_StreamWriteContext`
(класс принадлежит uvloop) и +35 МБ на `runners.py:118`. uvicorn запускался без указания
цикла и сам брал uvloop 0.22.1. Решение: `uvicorn.run(..., loop="asyncio")` — uvloop не
загружается вообще (проверено: 0 регионов в карте памяти процесса).

**Замер через 2 часа (25 точек наблюдателя, 12:15 -> 14:15 UTC):**

| режим | наклон RSS | heap | p50 |
|---|---|---|---|
| malloc + **uvloop** | +34.2 МБ/ч | 43 -> 120 МБ | 2.0 мс |
| pymalloc + **uvloop** (база) | +31.5 МБ/ч | 68 -> 131 МБ | 2.3 мс |
| pymalloc + **asyncio** (сейчас) | **+15.0 МБ/ч** | 19 -> **19 МБ** (не растёт) | **1.8 мс** |

За последний час: **+15.3 МБ/ч**. Здоровье: /health 200, сайт 200, пиры 6, msg/s 29.9,
p99 33 мс, uvloop в процессе 0 регионов.

**Промежуточный вывод:** uvloop оказался реальным фактором — рост упал ровно вдвое,
при этом быстрее НЕ стало хуже (p50 1.8 мс против 2.3). Но это не единственный источник:
остаток +15 МБ/ч не в Python-heap (он стабилен 19 МБ) — значит копится в анонимной
памяти C-слоя. Следующий шаг: срез анонимных регионов до/после 20 минут, сравнение
по конкретным регионам.

**Откат эксперимента №2 (если понадобится):** убрать `loop="asyncio"` из строки запуска
в `app.py` + `bash p2p_daily_restart.sh`. Бэкап до правки: `backups/app.py.2026-09-12-preloop`.

### РЕШЕНИЕ ПРИНЯТО (12.09, ~20:45 МСК): uvloop остаётся выключенным

`uvicorn.run(..., loop="asyncio")` — оставляем как есть. Обоснование по цифрам, не по вкусу:

| | uvloop включён | uvloop выключен |
|---|---|---|
| рост RSS | +31.5 МБ/ч | **+15.0 МБ/ч** |
| Python-heap | 68 → 131 МБ | **19 → 19 МБ** |
| p50 | 2.3 мс | **1.8 мс** |

**Что такое uvloop и зачем он вообще нужен.** Это альтернативная реализация цикла событий
asyncio поверх libuv (тот же движок, что в Node.js) — C вместо Python. Нужен ровно для одного:
больше сетевых операций в секунду под очень высокой нагрузкой (тысячи соединений, бенчмарки
показывают 2-4x на синтетике). Функциональных возможностей он НЕ добавляет.

**Что не работает при выключении: ничего.** `loop="asyncio"` — штатный цикл CPython.
Сокеты, websockets, TLS, таймеры, подпроцессы, сигналы, FastAPI/Starlette — всё на месте.
uvicorn поддерживает оба цикла равнозначно. Разница только в пропускной способности на пиках.

**Наша нагрузка:** 6 пиров, ~30 сообщений/с, p50 1.8 мс, один рабочий поток. На таких объёмах
uvloop не даёт ничего, зато давал 49 179 живых объектов `_StreamWriteContext` и вдвое больший
рост памяти. Для этого сервиса uvloop — чистый минус, а не оптимизация.

**Почему он вообще был включён:** uvicorn при `loop="auto"` сам подхватывает uvloop, если пакет
стоит в окружении. Это не решение проекта, а побочный эффект установки. Теперь цикл указан явно.

**Остаток:** пакет uvloop по-прежнему установлен в окружении — сам он не загружается и вреда не
несёт, но если где-то запустить сервер без явного `loop=`, он подхватится снова. Удаление пакета —
отдельное решение, не сделано.

**Откат (если понадобится):** убрать `loop="asyncio"` из строки запуска в `app.py`.
